__pycache__/
*.pyc
.env

# Personal memory-effectiveness eval — data stays LOCAL/PRIVATE (the cases + reports
# reference Tyler's internal claude-brain lessons). The memory_eval.py harness itself is
# generic tooling and stays tracked; only the personal data is ignored.
evals/memory/
evals/reports/memory-eff-*

.gitnexus

# [V3] scratch: per-(pattern,model) opus re-judge verdicts (resumable cache; not a deliverable)
.rejudge-cache/
